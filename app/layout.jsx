export const metadata = {
  title: "TapRank | NFC Google Review Stands",
  description: "Get more Google reviews with custom NFC and QR review stands for local businesses."
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0 }}>{children}</body>
    </html>
  );
}
