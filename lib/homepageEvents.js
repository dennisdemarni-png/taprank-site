/** Inactive integration seam: no network, logging, storage or personal data.
 * Add an explicitly approved analytics adapter here later. Callers pass fixed
 * UI identifiers only, never order details, links or contact information.
 */
export function homepageEvent(_name, _context = {}) {}
