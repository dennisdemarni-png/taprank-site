# Preserved homepage

`index.jsx` is the untouched homepage from commit `a3d15e4`, copied before the
conversion redesign. It is intentionally outside `pages/` and is not deployed.
For a complete rollback, restore `pages/index.jsx` and `lib/commerce.js` from that
commit; shared styles and customer/order route files have not been changed.
Historical prices in this archive are not the current offer and are never rendered
by the redesigned homepage. Do not use this archive as current commercial guidance.
